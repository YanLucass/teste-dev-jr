version: "3.3"

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "docker"
      POSTGRES_USER: "postgres"
      POSTGRES_DB: "desafio-cloudged"
    ports:
      - "5433:5432"
